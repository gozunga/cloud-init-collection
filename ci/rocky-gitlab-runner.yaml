#cloud-config
# Rocky/Alma: GitLab Runner (Docker executor).
# Replace registration values before use.

bootcmd:
  - |
    set -eu
    mirror_root='https://mirror.gozunga.com/rocky-linux'
    for f in /etc/yum.repos.d/rocky*.repo /etc/yum.repos.d/Rocky*.repo; do
      [ -f "$f" ] || continue
      sed -i -e 's/^[[:space:]]*mirrorlist=/# mirrorlist=/' \
             -e 's/^[[:space:]]*metalink=/# metalink=/' "$f"
      sed -i -e 's/^[[:space:]]*#[[:space:]]*baseurl=/baseurl=/' "$f"
      sed -i -e 's|https\?://[^/]*rockylinux\.org/\$contentdir|'"${mirror_root}"'|g' \
             -e 's|https\?://[^/]*rockylinux\.org/pub/rocky|'"${mirror_root}"'|g' \
             -e 's|https\?://dl\.rockylinux\.org/\$contentdir|'"${mirror_root}"'|g' "$f"
    done

package_update: true
package_upgrade: true

packages:
  - curl
  - ca-certificates

runcmd:
  - curl -fsSL https://get.docker.com | sh
  - systemctl enable --now docker
  - |
    set -e
    curl -fsSL https://packages.gitlab.com/install/repositories/runner/gitlab-runner/script.rpm.sh | bash
    dnf install -y gitlab-runner
    usermod -aG docker gitlab-runner || true
    # gitlab-runner register --non-interactive \
    #   --url "https://gitlab.example.com/" \
    #   --token "GLRT_REPLACE_ME" \
    #   --executor "docker" \
    #   --docker-image "docker:24" \
    #   --description "gozunga-runner" \
    #   --tag-list "docker,gozunga" \
    #   --run-untagged="true" \
    #   --locked="false"
    systemctl enable --now gitlab-runner

final_message: "GitLab Runner installed. Register with token (see commented runcmd)."
