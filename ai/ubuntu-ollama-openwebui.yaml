#cloud-config
# Ollama + Open WebUI on Ubuntu with NVIDIA GPU support
#
# Installs NVIDIA open-kernel drivers, NVIDIA Container Toolkit, Docker CE,
# and brings up Ollama + Open WebUI via Docker Compose.
#
# After boot (including driver reboot): http://<server-ip>:3000
# Ollama API: http://<server-ip>:11434
#
# Required security group ports:
#   - 22   (SSH — management)
#   - 3000 (Open WebUI)
#   - 11434 (Ollama API — optional, for direct model API access)
#
# Recommended: GPU-flavored instance (e.g. ac.nvrtx2000pro1 or larger)
# Tested on: Ubuntu 26.04 LTS

bootcmd:
  - |
    for f in /etc/apt/sources.list.d/ubuntu.sources /etc/apt/sources.list; do
      [ -f "$f" ] && sed -i \
        -e 's|https\?://archive\.ubuntu\.com/ubuntu|https://mirror.gozunga.com/ubuntu|g' \
        -e 's|https\?://security\.ubuntu\.com/ubuntu|https://mirror.gozunga.com/ubuntu|g' \
        -e 's|https\?://ports\.ubuntu\.com/ubuntu-ports|https://mirror.gozunga.com/ubuntu|g' \
        "$f" || true
    done

apt:
  primary:
    - arches: [default]
      uri: https://mirror.gozunga.com/ubuntu
  security:
    - arches: [default]
      uri: https://mirror.gozunga.com/ubuntu

package_update: true
package_upgrade: true

packages:
  - curl
  - ca-certificates

write_files:
  - path: /etc/modprobe.d/blacklist-nouveau.conf
    permissions: '0644'
    content: |
      blacklist nouveau
      blacklist lbm-nouveau
      options nouveau modeset=0
      alias nouveau off
      alias lbm-nouveau off

  - path: /opt/ollama/docker-compose.yml
    permissions: '0644'
    content: |
      services:
        ollama:
          image: ollama/ollama:latest
          container_name: ollama
          restart: unless-stopped
          ports:
            - "11434:11434"
          volumes:
            - ollama:/root/.ollama
          deploy:
            resources:
              reservations:
                devices:
                  - driver: nvidia
                    count: all
                    capabilities: [gpu]

        open-webui:
          image: ghcr.io/open-webui/open-webui:main
          container_name: open-webui
          restart: unless-stopped
          ports:
            - "3000:8080"
          volumes:
            - open-webui:/app/backend/data
          environment:
            - OLLAMA_BASE_URL=http://ollama:11434
          depends_on:
            - ollama

      volumes:
        ollama:
        open-webui:

  - path: /etc/systemd/system/ollama-webui.service
    permissions: '0644'
    content: |
      [Unit]
      Description=Ollama + Open WebUI (Docker Compose)
      Documentation=https://ollama.com https://openwebui.com
      After=docker.service network-online.target
      Requires=docker.service

      [Service]
      Type=oneshot
      RemainAfterExit=yes
      WorkingDirectory=/opt/ollama
      ExecStart=/usr/bin/docker compose up -d
      ExecStop=/usr/bin/docker compose down
      TimeoutStartSec=600
      Restart=on-failure

      [Install]
      WantedBy=multi-user.target

runcmd:
  - update-initramfs -u
  - add-apt-repository ppa:graphics-drivers/ppa -y
  - apt-get update -y
  - apt-get install -y nvidia-driver-595-server-open nvtop

  # Docker CE — official repository
  - |
    set -e
    install -m 0755 -d /etc/apt/keyrings
    curl -fsSL https://download.docker.com/linux/ubuntu/gpg -o /etc/apt/keyrings/docker.asc
    chmod a+r /etc/apt/keyrings/docker.asc
    . /etc/os-release
    cat > /etc/apt/sources.list.d/docker.sources <<SRC
    Types: deb
    URIs: https://download.docker.com/linux/ubuntu
    Suites: ${UBUNTU_CODENAME:-$VERSION_CODENAME}
    Components: stable
    Architectures: $(dpkg --print-architecture)
    Signed-By: /etc/apt/keyrings/docker.asc
    SRC
    sed -i 's/^[[:space:]]\+//' /etc/apt/sources.list.d/docker.sources
    apt-get update -y
    DEBIAN_FRONTEND=noninteractive apt-get install -y \
      docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
    systemctl enable docker

  # NVIDIA Container Toolkit — for GPU passthrough into Docker
  - |
    set -e
    curl -fsSL https://nvidia.github.io/libnvidia-container/gpgkey \
      | gpg --dearmor -o /usr/share/keyrings/nvidia-container-toolkit-keyring.gpg
    curl -s -L https://nvidia.github.io/libnvidia-container/stable/deb/nvidia-container-toolkit.list \
      | sed 's#deb https://#deb [signed-by=/usr/share/keyrings/nvidia-container-toolkit-keyring.gpg] https://#g' \
      | tee /etc/apt/sources.list.d/nvidia-container-toolkit.list
    apt-get update -y
    apt-get install -y nvidia-container-toolkit
    nvidia-ctk runtime configure --runtime=docker

  # Enable Compose service — starts automatically after reboot
  - systemctl daemon-reload
  - systemctl enable ollama-webui

power_state:
  mode: reboot
  message: "Rebooting to load NVIDIA drivers — Ollama + Open WebUI will start on next boot"
  timeout: 1

final_message: |
  NVIDIA drivers installed. Rebooting to load kernel module.
  After reboot, Ollama + Open WebUI will start automatically.
  Open WebUI: http://<server-ip>:3000
  Ollama API: http://<server-ip>:11434
